const { processRtcmFile } = require('./1_jiemi.js');

// 密钥与 nonce（与原 Python 脚本完全一致的值）
const KEY = Buffer.from([
    0x12, 0x34, 0x56, 0x78, 0x91, 0x23, 0x45,
    0x67, 0x89, 0x12, 0x34, 0x56, 0x78, 0x9A,
    0xAA, 0xAF
]);
const NONCE = Buffer.from([
    0x12, 0x34, 0x56, 0x78, 0x91, 0x23, 0x45,
    0x67, 0x89, 0x12, 0x34, 0x56, 0x00, 0x00,
    0x12, 0x34
]);

// 文件路径（假设与脚本同目录）
processRtcmFile(
    'ENRTCM-0508.log',        // 输入原始文件
    'valid_rtcm.rtcm',   // 中间文件（可设为 null 跳过保存）
    'extracted.rtcm',    // 最终解密文件
    KEY,
    NONCE
);

